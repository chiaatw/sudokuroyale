[package]
name = "sudokuroyale"
version = "0.1.0"
edition = "2024"

[dependencies]
uuid = { version = "1", features = ["v4"] }
chrono = { version = "0.4", features = ["clock"] }
argon2 = "0.5"
rand_core = "0.6"
regex = "1"